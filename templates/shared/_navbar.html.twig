<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">Symshop</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse d-flex justify-content-between" id="navbarColor01">
      <ul class="navbar-nav mr-auto">
        {% for c in categoryRepository.findAll() %}
            <li class="nav-item">
                <a href="{{ path('product_category', {'slug': c.slug}) }}" class="nav-link">{{ c.name }}</a>
            </li>
        {% endfor %}
      </ul>
      <ul class="navbar-nav align-left">
        <li class="nav-item"><a href="{{ path('cart_show') }}" class="nav-link"><i class="fas fa-shopping-cart"> Panier {{ cartService.getTotal() }}</i></a></li>
        <li class="nav-item">
            <a href="" class="nav-link">Inscription</a>
        </li>
        {% if app.user %}
          <li class="nav-item">
              <a href="{{ path('app_logout') }}" class="btn btn-danger">Logout</a>
          </li>
        {% else %}
          <li class="nav-item">
              <a href="{{ path('app_login') }}" class="btn btn-success">Login</a>
          </li>
        {% endif %}
      </ul>
    </div>
  </div>
</nav>