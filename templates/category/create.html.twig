{% extends "base.html.twig" %}

{% block title %}
    Créer une catégorie
{% endblock %}

{% block body %}
    <h1>Créer une nouvelle Catégorie</h1>

    {{form_start(formview)}}
    {{form_errors(formview)}}

    <div class="row">
        <div class="col">
            {{form_row(formview.name)}}
        </div>
    </div>

    <button class="btn btn-primary">
        <i class="fas fa-save"> Créer la catégorie</i>   
    </button>
{% endblock %}